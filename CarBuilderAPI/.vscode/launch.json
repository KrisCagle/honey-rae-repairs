{
    "version": "0.2.0",
    "configurations": [
        {
            "name": ".NET Core Launch (web)",
            "type": "dotnet",
            "request": "launch",
            "projectPath": "${workspaceFolder}/CarBuilderAPI.csproj"
        }
    ]
}